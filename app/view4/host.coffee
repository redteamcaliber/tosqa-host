module.exports = (app, plugin) ->
  app.on 'setup', ->
    

    # primus.on 'connection', (spark) ->
    # primus.on 'connection', (spark) ->
      # spark.on 'addMotor', () ->
        # # # spark.write['nMotors', counter]
        # counter ++